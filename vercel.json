{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "rootDirectory": "TestBilder",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    },
    "app/Api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  },
  "routes": [
    {
      "src": "/Api/(.*)",
      "dest": "/Api/$1"
    },
    {
      "src": "/Api/(.*)",
      "dest": "/Api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "rewrites": [
    {
      "source": "/uploads/(.*)",
      "destination": "/Api/uploads/$1"
    }
  ]
}
